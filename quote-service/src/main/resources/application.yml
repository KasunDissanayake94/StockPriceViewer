spring:
  application:
    name: quote-service

server:
  port: 8303

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:8302/eureka/
  instance:
    hostname: localhost

