spring:
  application:
    name: stock-service

server:
  port: 8301

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:8302/eureka/
  instance:
      hostname: localhost

